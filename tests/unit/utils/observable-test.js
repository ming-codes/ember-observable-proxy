import observable from 'dummy/utils/observable';
import { module, test } from 'qunit';

module('Unit | Utility | observable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = observable();
  assert.ok(result);
});
